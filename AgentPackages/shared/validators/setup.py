from setuptools import setup, find_packages

setup(
    name='volttron-validators',
    version='0.1',
    packages=find_packages(),
    description='Shared lib to validate control commands for plants',
    author='Dany',
    install_requires=[],
)