from setuptools import setup, find_packages

setup(
    name='volttron-persistance',
    version='0.1',
    packages=find_packages(),
    description='Shared lib to persist data in agents',
    author='Dany',
    install_requires=[],
)